package com.example.Health.data.repositories;

import com.example.Health.models.ProgramNews;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProgramNewsRepository extends JpaRepository<ProgramNews, Long> {
}
